# Maps

Spatial outputs live here. `layers/` is reserved for layer definitions or small derived spatial artefacts appropriate for Git.

EXP-001 should determine the minimum mapping approach after authoritative source formats are understood. No GIS, web-map, tiling or hosting stack is selected at bootstrap.
