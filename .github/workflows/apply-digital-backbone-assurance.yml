name: Apply digital backbone assurance batch
on:
  push:
    branches: [feature/ingest-digital-backbone-001]
    paths:
      - "assurance/ingestion_batches/digital_backbone_2026.json"
      - ".github/workflows/apply-digital-backbone-assurance.yml"
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {ref: feature/ingest-digital-backbone-001, fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - run: python tools/apply_assurance_batch.py assurance/ingestion_batches/digital_backbone_2026.json
      - run: python tools/audit_current_datasets.py
      - run: python tools/validate_dataset_register.py
      - run: python tools/validate_source_assurance.py --strict
      - name: Publish canonical assurance state
        run: |
          git config user.name "VECA assurance bot"
          git config user.email "actions@users.noreply.github.com"
          git add assurance/source_register.json assurance/dataset_register.json assurance/curated_dataset_contracts.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Assurance: register digital backbone dataset"
          git pull --rebase --autostash origin feature/ingest-digital-backbone-001
          git push origin HEAD:feature/ingest-digital-backbone-001
