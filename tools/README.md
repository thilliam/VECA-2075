# Tools

Add tooling only when a research task requires it. Prefer the smallest reproducible mechanism that answers the current question.

No GIS package, database, programming language, application framework, web stack, optimisation library or visualisation platform is selected at bootstrap.
